$('#show_modal').html("<%= j render partial: 'modal_locked_chatroom', locals: {chatroom: @chatroom} %>");
$('#chatroom_modal').modal();
$('#chatroom_modal').on('shown.bs.modal', function() {
  $('#password').focus();
})